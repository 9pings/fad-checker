# Tests & fixtures
test/

# Documentation (README.md + CHANGELOG.md are still published)
docs/
CLAUDE.md

# Compiled single-file binaries — shipped as GitHub release assets, not on npm
dist/

# Dev / editor / agent state
.idea/
.vscode/
.claude/
.github/

# Working documents that must never ship (untracked in git, but npm packs the
# working directory, not the index)
launch-kit.md
rr2.json
*.local.md

# Misc
*.tgz
.DS_Store
Thumbs.db
